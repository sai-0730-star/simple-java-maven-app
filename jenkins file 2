node{
stage('SCM Checkout'){
git 'https://github.com/sai-0730-star/simple-java-maven-app'
}
stage('Compile-Package'){
def mvnHome = tool name: 'maven3', type: 'maven'
sh "${mvnHome}/bin/mvn package"
}
}
